<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="info.tommarsh.eventsearch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="info.tommarsh.eventsearch.EventSearchApp"
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventSearch">
        <activity
            android:name="info.tommarsh.eventsearch.ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.EventSearch"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <data
                    android:host="eventsearch.app"
                    android:scheme="app" />
            </intent-filter>
        </activity>

        <receiver android:name=".core.notifications.NotificationReceiver" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="info.tommarsh.eventsearch.startup.NotificationInitialiser"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>